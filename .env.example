# After `npx wrangler deploy` in /ziscus, paste your worker submit URL:
# https://ziscus-ask.<your-subdomain>.workers.dev/submit
PUBLIC_ZISCUS_SUBMIT_URL=

# Used by `npx ziscus` CLI / admin dashboard (never commit the real value)
ZISCUS_ADMIN_SECRET=
